[project]
name = "dify-trace-langsmith"
version = "0.0.1"
dependencies = [
    "langsmith==0.8.5",
]
description = "July ops tracing provider (LangSmith)."

[tool.setuptools.packages.find]
where = ["src"]
